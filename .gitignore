.venv/
__pycache__/
*.pyc
.DS_Store
.env
.env.*
!.env.example

# generated chart artifacts during early prototyping
tencent_kline.png
nvda_kline.png

# locally installed claude skills (sourced from moomoo zip; not project source)
.claude/skills/

# editor / OS noise
.idea/
.vscode/

# playwright MCP session artifacts
.playwright-mcp/

# build artifacts from scripts/build-web-image.sh
dist/
.claude/scheduled_tasks.lock

# RSA key shared with OpenD for encrypted trade channel — never commit.
secrets/
apps/api/secrets/
